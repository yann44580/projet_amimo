{% extends 'admin.html.twig' %}

{% block title %}Les fiches créations{% endblock %}

{% block body %}
    <h1>Fiche création</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tool.id }}</td>
            </tr>
            <tr>
                <th>Tool_title</th>
                <td>{{ tool.toolTitle }}</td>
            </tr>
            <tr>
                <th>Tool_content</th>
                <td>{{ tool.toolContent }}</td>
            </tr>
            <tr>
                <th>Tool_picture</th>
                <td>{{ tool.toolPicture }}</td>
            </tr>
            <tr>
                <th>Tool_publication_date</th>
                <td>{{ tool.toolPublicationDate ? tool.toolPublicationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Tool_author</th>
                <td>{{ tool.toolAuthor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_tools_creation_index') }}">back to list</a>

    <a href="{{ path('admin_tools_creation_edit', {'id': tool.id}) }}">edit</a>

    {{ include('admin/tools_creation/_delete_form.html.twig') }}
{% endblock %}
