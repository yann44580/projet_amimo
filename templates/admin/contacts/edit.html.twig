{% extends 'admin.html.twig' %}

{% block title %}Edit Contacts{% endblock %}

{% block body %}
    <h1>Edit Contacts</h1>

    {{ include('admin/contacts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_contacts_index') }}">back to list</a>

    {{ include('admin/contacts/_delete_form.html.twig') }}
{% endblock %}
