{% extends 'admin.html.twig' %}

{% block title %}Edit PopulationsType{% endblock %}

{% block body %}
<div class="administration">
    <fieldset class="container">
        <h1>Edit PopulationsType</h1>

        {{ include('admin/populations_type/_form.html.twig', {'button_label': 'Update'}) }}

        {# <a href="{{ path('admin_populations_type_index') }}">back to list</a> #}

        {{ include('admin/populations_type/_delete_form.html.twig') }}
        </div>
    </fieldset>
</div>
{% endblock %}
