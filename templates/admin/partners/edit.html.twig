{% extends 'admin.html.twig' %}

{% block title %}Modifier partenaires
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier les données partenaires</h1>

		{{ include('admin/partners/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('admin_partners_index') }}">Revenir à la liste</a>

		{{ include('admin/partners/_delete_form.html.twig') }}
	</div>
{% endblock %}
