{% extends 'admin.html.twig' %}

{% block title %}Animals{% endblock %}

{% block body %}
    <h1>Animals</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Animal_name</th>
                <td>{{ animal.animalName }}</td>
            </tr>
            <tr>
                <th>Animal_picture</th>
                <td>{{ animal.animalPicture }}</td>
            </tr>
            <tr>
                <th>Animal_content</th>
                <td>{{ animal.animalContent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_animals_index') }}">back to list</a>

    <a href="{{ path('admin_animals_edit', {'id': animal.id}) }}">edit</a>

    {{ include('admin/animals/_delete_form.html.twig') }}
{% endblock %}
