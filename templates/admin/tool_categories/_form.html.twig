{{ form_start(form) }}
<div class="input-field">
	{{ form_label(form.tool_category_name) }}
	{{ form_widget(form.tool_category_name) }}
</div>
<div class="confirm_select">
<a href="{{ path('admin_tool_categories_index') }}"><p>Retour à la liste</p><div class="btn blue-grey return"><img src="{{ asset('/uploads/icone/fleche_retour.png') }}" alt="fleche retour"></div></a>

<a href=""><p>Enregistrer les modifications</p><button class="btn teal lighten-1"><img src="{{ asset('/uploads/icone/save1.png') }}" alt=""></button></a>
{# <button class="btn">{{ button_label|default('Enregistrer') }}</button> #}
{{ form_end(form) }}
