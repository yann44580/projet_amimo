<form method="post" action="{{ path('admin_blogs_delete', {'id': blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
<a href=""><p>Supprimer</p><button class="btn red darken-3"><i class="far fa-trash-alt"></i></button></a>
</form>
