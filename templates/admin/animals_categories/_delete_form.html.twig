<form method="post" action="{{ path('admin_animals_categories_delete', {'id': animals_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animals_category.id) }}">
    <button class="btn">Delete</button>
</form>
