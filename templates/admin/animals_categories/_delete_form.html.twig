<form method="post" action="{{ path('admin_animals_categories_delete', {'id': animals_category.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animals_category.id) }}">
 
<a href=""><p>Supprimer</p><button class="btn red darken-3"><i class="far fa-trash-alt"></i></button></a>
</form>
