{{ form_start(form) }}
<div class="input-field">
	{{ form_label(form.animal_category_name) }}
	{{ form_widget(form.animal_category_name) }}
</div>
{# <div class="input-field">
	{{ form_row(form.tools) }}
</div> #}
		
<div class="confirm_select">
<a href="{{ path('admin_animals_categories_index') }}"><p>Retour à la liste</p><button class="btn blue-grey return"><img src="{{ asset('/uploads/icone/fleche_retour.png') }}" alt="fleche retour"></button></a>
<a href=""><p>Enregistrer les modifications</p><button class="btn">{{ button_label|default('Sauvegarder') }}</button></a>
{{ form_end(form) }}
