<form method="post" action="{{ path('admin_populations_delete', {'id': population.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ population.id) }}">
    <button class="btn">Delete</button>
</form>
