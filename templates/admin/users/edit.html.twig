{% extends 'admin.html.twig' %}

{% block title %}Modification des données
{% endblock %}

{% block body %}
<div class="administration">
	<fieldset class="container">
		<h1>Modification des données personnelles</h1>

		{{ include('admin/users/_form.html.twig', {'button_label': 'Update'}) }}

		{# <a href="{{ path('admin_users_index') }}">revenir à la liste</a> #}
 
		{{ include('admin/users/_delete_form.html.twig') }}
		</div>
	</fieldset>
</div>
{% endblock %} 
