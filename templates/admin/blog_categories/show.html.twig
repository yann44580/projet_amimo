{% extends 'base.html.twig' %}

{% block title %}BlogCategories{% endblock %}

{% block body %}
    <h1>BlogCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog_category.id }}</td>
            </tr>
            <tr>
                <th>Blog_category_name</th>
                <td>{{ blog_category.blogCategoryName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_blog_categories_index') }}">back to list</a>

    <a href="{{ path('admin_blog_categories_edit', {'id': blog_category.id}) }}">edit</a>

    {{ include('admin/blog_categories/_delete_form.html.twig') }}
{% endblock %}
