<form method="post" action="{{ path('admin_blog_categories_delete', {'id': blog_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_category.id) }}">
<a href=""><p>Supprimer</p><button class="btn red darken-3"><i class="far fa-trash-alt"></i></button></a>
</form>
