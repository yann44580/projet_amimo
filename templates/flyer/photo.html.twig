
{% for association in associations %}   
	{# {% for image in associations.Association_logo %}	  #}
	{# <pre>
		{{ dump(associations) }}
	</pre> #}
			<img src="{{ asset('uploads/' ~ association.associationlogo) }}" alt="" width="80">  
	 {# {% endfor %}  #}
{% endfor %}    
